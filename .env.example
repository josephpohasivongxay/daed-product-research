# Optional: enables higher-quality store discovery via Google Programmable Search
# (Custom Search JSON API). If unset, the app falls back to a free DuckDuckGo
# HTML scrape, and finally to a small sample domain list if that also fails.
# https://developers.google.com/custom-search/v1/overview
GOOGLE_CSE_KEY=
GOOGLE_CSE_ID=
